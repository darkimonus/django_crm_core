# --- Core backend ---
Django>=5.0,<6.0
djangorestframework>=3.15,<4.0
django-filter>=24.0,<26.0
psycopg[binary]>=3.1,<4.0
django-environ>=0.11,<1.0
python-dateutil>=2.8,<3.0

# --- API docs (OpenAPI/Swagger/Redoc) ---
drf-spectacular>=0.27
drf-spectacular-sidecar>=2024.1.0

# --- Auth ---
djangorestframework-simplejwt>=5.3

# --- WSGI ---
gunicorn>=21.2

# --- Testing ---
pytest>=8.0
pytest-django>=4.7
# pytest-cov>=4.1
# model-bakery>=1.18
# freezegun>=1.2

# --- (Optional) Performance/Ingest ---
# orjson>=3.10        # fast JSON serializer (for ingest services)
# pyarrow>=16.0       # if you plan to use parquet/large batch through Parquet
# pandas>=2.2         # if convenient to read CSV/Parquet through pandas

# --- (Optional) Code style dev utilities ---
# black>=24.0
# isort>=5.13
# ruff>=0.5
